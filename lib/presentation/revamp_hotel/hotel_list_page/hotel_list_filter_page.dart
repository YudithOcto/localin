import 'package:flutter/material.dart';

class HotelListFilterPage extends StatelessWidget {
  static const routeName = 'HotelListFilterPage';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
